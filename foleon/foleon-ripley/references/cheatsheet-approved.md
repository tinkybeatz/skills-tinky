# ripley — approved cheat-sheet rows (GENERATED)

> **Do not hand-edit.** Regenerate with `foleon-cheatsheet` ("pull the approved rows"). Any manual
> change here is lost on the next pull — and the Notion row is the source, not this file.

The maintainer-approved subset of the Notion `Findings` database: symptom → fix, already written to
be scanned. This is the **third context layer**, and the most trustworthy of the three because a
human verified every line:

| Layer | What it is |
|---|---|
| `project-facts.md` | curated stable facts — stack, layout, commands, conventions |
| `knowledge.md` | the authoritative dated log, dense and complete, machine-facing |
| **this file** | human-verified symptom → fix rows, pulled from Notion |

Each row keeps its `CS-nn` id, so it can be traced back to the Notion row and to the log entry that
produced it.

Last pulled: 2026-08-03

## No approved rows yet

All 18 rows are currently `Status = Needs review` — they are waiting in the **Review queue** view of
the Notion database. Nothing has been approved, so there is nothing to load here yet.

This is the expected state on day one, not an error: the review gate exists precisely so nothing
reaches this file without the maintainer's sign-off. Approve a batch in Notion, then ask
`foleon-cheatsheet` to pull, and the rows appear below grouped by area.
