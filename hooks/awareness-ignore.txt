# Repos opted out of the project-awareness nudge — optionally bound to a context skill.
# One entry per line. '#' starts a comment. Two forms:
#   <repo>                 opt out of the "create a CLAUDE.md" nudge
#   <repo> -> <skill>      opt out AND point at the skill that holds this project's context
# <repo> is a bare folder name (PORTABLE across machines) or an absolute path (this machine only).
# Prefer names; this file is committed and synced across your laptops.
#
# Easiest way to add the current repo (both append + auto-commit ONLY this file):
#   hooks/awareness-ignore-add.sh . <skill>     bind current repo to a context skill
#   hooks/awareness-ignore-add.sh               plain opt-out, current repo

ripley -> foleon-ripley
