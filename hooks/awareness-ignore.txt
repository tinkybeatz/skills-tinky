# Repos where the project-awareness nudge should NOT fire.
# One entry per line. '#' starts a comment. Two forms:
#   - bare repo folder name  (e.g. `ripley`)  → PORTABLE: matches on any machine
#   - absolute path          (e.g. /Users/me/work/ripley) → this machine only
# Prefer names; this file is committed and synced across your laptops.
#
# Add the current repo the easy way: `hooks/awareness-ignore-add.sh`
# (appends by name + auto-commits). Or edit this file by hand.

ripley
